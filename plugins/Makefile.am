
if WITH_PYTHON
PYTHON = python
else
PYTHON = 
endif

if WITH_PERL
PERL = 
else
PERL =
endif

if GUILE
GUILE_DIR = guile
else
GUILE_DIR =
endif

SUBDIRS = sample $(PYTHON) $(PERL) stat excel $(GUILE_DIR)
