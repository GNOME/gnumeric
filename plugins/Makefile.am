
if WITH_PYTHON
PYTHON = python
else
PYTHON = 
endif

if WITH_PERL
PERL = perl
else
PERL =
endif

if GUILE
GUILE_DIR = guile
else
GUILE_DIR =
endif

SUBDIRS = sample excel lotus-123 oleo sc sylk ff-csv text xbase html dif ff-stf $(PYTHON) $(GUILE_DIR)
